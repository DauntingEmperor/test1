name: vortex_launcher
description: A Flutter app mimicking the iOS home screen with a custom app store.

environment:
  sdk: '>=2.12.0 <3.0.0'  # Adjust the SDK constraint to match your setup

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/backgrounds/
